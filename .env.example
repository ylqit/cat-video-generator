# Ark：标准API与Agent Plan必须整组切换URL、Key和模型。
ARK_API_KEY=
ARK_ACCESS_MODE=standard
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
ARK_IMAGE_MODEL=doubao-seedream-5-0-260128
ARK_VIDEO_MODEL=doubao-seedance-2-0-mini-260615
# 调用文本模型思考推理剧本内容
ARK_PLANNING_MODEL=doubao-seed-2-1-pro-260628
ARK_REVIEW_MODEL=doubao-seed-2-1-pro-260628
ARK_RESPONSES_STRUCTURED_OUTPUT_MODE=json_object_schema_prompt
ARK_VIDEO_RESOLUTION=720p
ARK_POLL_INTERVAL_SECONDS=10
ARK_TASK_TIMEOUT_SECONDS=1800
DAILY_PLAN_CANDIDATE_COUNT=1
CAT_VIDEO_EVENT_SEED_ROOT=content/events
# semantic_auto通过Ark审核身份、二维画风和可见世界；低置信转人工。
# technical_auto仅供实验，run-day还必须显式提供--allow-unverified-keyframes。
KEYFRAME_REVIEW_MODE=semantic_auto
VIDEO_SEMANTIC_REVIEW_MODE=diagnostic

# 媒体工具：留空时从PATH发现。
FFMPEG_PATH=
FFPROBE_PATH=
MEDIA_WORK_ROOT=var/work
MEDIA_ASSET_ROOT=var/assets
DELIVERY_OUTPUT_ROOT=output

# PostgreSQL：正式环境优先使用require或安全隧道。
CAT_VIDEO_DB_HOST=
CAT_VIDEO_DB_PORT=5432
CAT_VIDEO_DB_NAME=vedio-appdb
CAT_VIDEO_DB_USER=postgres
CAT_VIDEO_DB_PASSWORD=
CAT_VIDEO_DB_SSLMODE=require
CAT_VIDEO_DB_SCHEMA=cat_video
CAT_VIDEO_ALLOW_INSECURE_RUNTIME=false
CAT_VIDEO_ALLOW_INSECURE_READONLY_SMOKE=false
三份分析全部到齐。现在汇总为最终的批判性报告与修复优先级，写入计划文件：

Update Todos

批判分析：领域契约层（continuity/visual_profiles/contracts/prompts/rules/media）

批判分析：应用与基础设施层（planning/production/assets/gateway/db/cli）

批判分析：测试覆盖与文档一致性

汇总三份分析，产出总体健壮性判断与修复优先级