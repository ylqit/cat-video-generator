# LibTV 节点交互重构视觉验收

## 验收基准

- 视觉真值：`C:\Users\wwwab\AppData\Local\Temp\codex-clipboard-a188d189-1717-4451-b8e8-461cc13a9176.png`
- 真值尺寸：1954 × 1047
- 目标状态：选中图片生成节点，节点本体展示可执行能力，节点下方打开局部生成控制台。
- 重点比较：完整画布高度、节点与局部控制台的相对位置、暗色层级、边框与圆角、输入区域、模型与参数栏、主操作按钮、节点失焦行为。

## 实现证据

- 生产构建：`npm --prefix web run build` 通过。
- 前端组件测试：70 个测试通过。
- 后端测试：210 个测试通过。
- TypeScript 类型检查和 Ruff 检查通过。
- 局部控制台、脚本三入口、三步全屏工作流、动作契约和异步任务状态均已有自动测试覆盖。

## 浏览器对比

- 实现截图：不可用。
- 全视图同视口对比：未执行。
- 聚焦节点对比：未执行。
- 交互复核：未执行。

原因：按用户指定应使用 Chrome，但本次 Codex Chrome 自动化桥接在初始化内核资产时返回“系统找不到指定的路径”，无法打开生产构建、设置同视口状态或采集实现截图。依据视觉验收规则，静态代码检查和构建结果不能替代真实浏览器截图对比，也不能据此宣称视觉一致。

## 待完成项

1. 恢复 Chrome 自动化桥接后，以 1954 × 1047 打开生产构建。
2. 选中图片生成节点并展开局部控制台，采集全视图和节点聚焦截图。
3. 将实现截图与视觉真值并排比较，修正可见的尺寸、间距、裁切、字体、边框和圆角差异。
4. 复核点击空白失焦、Space 平移保留选择、Esc 分层退出、三种脚本入口和任务进度状态。

## 结论

final result: blocked
